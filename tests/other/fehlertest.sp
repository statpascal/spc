program dummy;

type 
    t = integer;
    t = integer;
    t = real;
    t = undefined;

function test (a: integer): integer;
    type 
        t = string;
    begin
        test := a
    end;

begin
    a := -b;
    a := not b;
    a := not not not test;
    a := g (h);
    a := test (h);
    a := b^ * b^^;
    x = y.z xor e [f]
    y := sin (x);
    writeln (a, b, c);
    if expr then stm1 else h;
    repeat
    until expr;
    while expr do
        for a := b to c do;
    if a then b else
